name: jielong
version: 1.0.0+1
publish_to: none

description: 成语接龙休闲游戏 - Flutter 客户端

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0
  flutter_bloc: ^8.1.0
  flutter_svg: ^2.0.0
  audioplayers: ^5.0.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/idioms.json
